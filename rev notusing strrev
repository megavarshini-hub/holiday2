#include <stdio.h>

int main()
{
    char ch[50];
    int len=0,i;//apple
    

    printf("enter the word:");
    scanf(" %s", ch);
    while (ch[len] != '\0') //ch[apple]!= 
    {
       len++;
    }
     for (i = len - 1; i >= 0; i--) {
        printf("%c", ch[i]);
    }
    return 0;
}
